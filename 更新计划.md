# 更新计划

## 已完成 ✅（v0.9，2026-08-09）
- [x] **TTS 语音合成**（疯ˣ）— edge-tts 即时合成，无需训练，开箱即用
- [x] **声音克隆**（疯ˣ）— GPT-SoVITS v2 一键安装/训练/推理，内置国内镜像降级
- [x] **Hermes Agent 一键部署**（疯ˣ）— hermes_setup.py 自动探测/自动安装/内核直连
- [x] **记忆引擎内嵌 + 镜像降级**（主创）— vendor/ 内嵌 universal-agent-memory v3.0.2，模型下载自动切 hf-mirror
- [x] **本地 STT 语音输入**（疯ˣ）— sherpa-onnx zipformer-zh 14M，一键安装 + 麦克风识别，与 TTS 形成语音闭环
- [x] **模式切换 UI**（主创）— 前端陪伴/工作切换按钮，对接 /api/mode
- [x] **声音克隆选装提示**（主创）— 安装前明示资源占用（下载量/磁盘/耗时），默认微软电子音免安装
- [x] **默认 GUI 窗口模式**（主创）— python app.py 即独立窗口，--web 切浏览器
- [x] **UI 去品牌化**（主创）— Hermes→Agent 内核，文档鸣谢
- [x] **数字形象**（主创）— 内置 SVG 月亮精灵 + Live2D 多模型切换（Miyara/Kei）+ 语音对口型
- [x] **桌宠模式**（主创）— `python app.py --pet` 透明悬空小夜，可拖拽；右键弹**独立对话窗口**（1000×700 完整 UI）/ 置顶切换 / 打招呼 / 退出；卡片对话升级为独立窗口

## 待办（按优先级）

1. **移动端适配** — 前端是 880px 桌面布局，手机浏览器体验一般
2. **打包部署** — Docker 或 systemd，方便换机器部署
3. **声音克隆增强** — 训练进度可视化、多音色切换、克隆质量优化

---

## 📝 本次更新说明（桌宠模式，2026-08-10）

### 新增：桌宠模式 `python app.py --pet`

让 Live2D 小夜「悬空」在桌面上，像真人一样陪着你。

**核心能力：**
- **透明悬空窗口**：无边框 + 透明背景 + 置顶，只有小人在桌面上，不占任务栏
- **随意拖拽**：`easy_drag` 按住小人拖到屏幕任意位置
- **右键菜单**：💬 说句话 / 🖥️ 完整面板 / 📌 置顶切换 / 👋 打招呼 / ✕ 退出

**对话窗口（本次升级）：**
- 右键「说句话」→ 弹出**独立对话窗口**（1000×700，完整聊天 UI）
- 不再使用小窗内卡片（太小不便使用），独立窗口包含全部功能：文本聊天 / TTS 语音 / STT 语音输入 / 记忆面板 / 设置 / 模式切换
- 对话窗口关闭后小夜继续在桌面

**技术实现：**
- `web/pet.html`（桌宠页面：Live2D 渲染 + 右键菜单 + 拖拽）
- `app.py --pet` 分支（透明窗口 + PetApi js_api + 预创建隐藏对话窗口）
- 对话窗口隐藏预创建，右键时 `show() + restore()` 弹出

**修复的 Bug：**
- pywebview `background_color` 不支持 8 位 hex（`#00000000`）→ 改 6 位 `#000000`
- 桌宠窗口 API 请求 401 → `--pet` 模式自动禁用鉴权（本机 127.0.0.1）
- pywebview 窗口没有 `focus()` 方法（报 `'bool' object is not callable`）→ 改 `show() + restore()`

### 待办（下一步）

1. **桌宠增强**：双击小夜聊天、右键菜单自定义、多形象快捷切换
2. **打包 exe**：PyInstaller 打包桌宠/主窗口，双击即用可分发

（CI 已去除，不列入计划）
